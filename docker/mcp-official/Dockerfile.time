# MCP Time Server with mcp-proxy (stateless HTTP mode) + ToolTimer profiling
#
# Uses mcp-proxy to expose stdio-based MCP server over HTTP
# Patched with ToolTimer for timing measurements
#
# Usage:
#   docker build -f Dockerfile.time -t edgeagent-mcp-time .
#   docker run -p 8080:8080 edgeagent-mcp-time

FROM python:3.11-slim

WORKDIR /app

# Install Node.js for mcp-proxy
RUN apt-get update && apt-get install -y curl && \
    curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && \
    apt-get install -y nodejs && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

# Install mcp-proxy (Node) and time server (Python)
RUN npm install -g mcp-proxy && \
    pip install --no-cache-dir mcp-server-time

# Copy patched files with ToolTimer
COPY servers/patches/python/timing.py /usr/local/lib/python3.11/site-packages/mcp_server_time/
COPY servers/patches/python/time_server_patched.py /usr/local/lib/python3.11/site-packages/mcp_server_time/server.py

# Environment variables
ENV MCP_PORT=8080

# Expose HTTP port
EXPOSE 8080

# Run mcp-proxy in stateless mode
CMD ["sh", "-c", "mcp-proxy --port ${MCP_PORT} --stateless mcp-server-time"]
