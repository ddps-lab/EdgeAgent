# 외부 인터넷 대역폭 (Mbps) - 티어별
external_bandwidth_mbps:
  DEVICE: 100
  EDGE: 10
  CLOUD: 1000

# ============================================================================
# Network Measurements (실측 기반, 티어별)
# ============================================================================
# 대표 하드웨어: device-rpi, edge-nuc, cloud-c7i48xlarge
network_measurements:
  bandwidth_mbps:
    D_E: 943.71    # edge-nuc ↔ device-rpi
    D_C: 325.66    # device-rpi ↔ cloud
  latency_ms:
    D_E: 0.892     # edge-nuc ↔ device-rpi
    D_C: 4.329     # device-rpi ↔ cloud

# Hardware Mapping: 하드웨어
hardware_mapping:
  DEVICE: [device-rpi]
  EDGE: [edge-nuc]
  CLOUD: [cloud-c7i48xlarge]
